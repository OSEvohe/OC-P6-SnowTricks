{% extends 'base.html.twig' %}

{% block title %}S'enregistrer - {{ parent() }}{% endblock %}

{% block body %}

    <section class="container py-5">
        <div class="row">
            <div class="col-sm-12 col-md-9 col-lg-6 m-auto bg-dark text-light p-5">

                {{ form_start(registrationForm, {'attr': {'novalidate': 'novalidate'}}) }}
                <div class="form-group">
                    {{ form_label(registrationForm.email,'Adresse Mail') }}
                    <div class="form-text alert-danger">{{ form_errors(registrationForm.email) }}</div>
                    {{ form_widget(registrationForm.email,{'attr': {'class' : 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_label(registrationForm.password,'Mot de passe') }}
                    <div class="form-text alert-danger">{{ form_errors(registrationForm.password) }}</div>
                    {{ form_widget(registrationForm.password,{'attr': {'class' : 'form-control'}}) }}
                </div>
                <div class="form-group">
                    {{ form_label(registrationForm.displayName,'Pseudo') }}
                    <div class="form-text alert-danger">{{ form_errors(registrationForm.displayName) }}</div>
                    {{ form_widget(registrationForm.displayName,{'attr': {'class' : 'form-control'}}) }}
                </div>

                <button class="btn btn-lg btn-primary btn-block" type="submit">
                    S'enregistrer
                </button>
                {{ form_end(registrationForm) }}
            </div>
        </div>
    </section>
{% endblock %}
